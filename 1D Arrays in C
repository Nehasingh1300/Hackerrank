#include <stdio.h>
#include <stdlib.h>

int main() {
    int n;
    // i, arr;
    int *ptr;
    int sum=0;
    
    scanf("%d",&n);
    ptr = ( int* ) malloc (n * sizeof(int));

    for(int i=1; i<=n; i++){
        scanf("%d",ptr);
       

    //for(int j=1; j<=n; j++){
        sum = sum + *ptr;
    //}
    }
    printf("%d", sum);    
     
    return 0;
}

